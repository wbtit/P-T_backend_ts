import { ModuleOpenApiDoc } from "../../openapi/types";

export const uploadsOpenApiDoc: ModuleOpenApiDoc = {
  tag: {
    name: "Uploads",
    description: "API endpoints for Uploads module"
  },
  paths: {
    // This module currently has no HTTP routes mounted in app.ts.
  }
};

export default uploadsOpenApiDoc;
